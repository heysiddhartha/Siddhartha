let items=[];let filter="all";const feed=document.querySelector("#feed"),search=document.querySelector("#search"),count=document.querySelector("#count"),empty=document.querySelector("#empty");
function render(){const q=search.value.toLowerCase().trim();const out=items.filter(x=>(filter==="all"||x.type===filter)&&(!q||[x.title,x.company,x.location,x.type,x.source,x.categories].join(" ").toLowerCase().includes(q)));count.textContent=items.length;feed.innerHTML=out.map(x=>`<article class="card"><div class="meta"><span class="tag">${x.type}</span><span>${x.date||""}</span></div><h3>${x.title}</h3><div class="company">${x.company}</div><div class="location">${x.location||"India"} · ${x.mode||"See listing"}</div><a href="${x.url}" target="_blank" rel="noopener">View opportunity ↗</a></article>`).join("");empty.hidden=out.length!==0}
fetch("data/opportunities.json").then(r=>r.json()).then(d=>{items=d;render()}).catch(()=>{items=[];render()});
document.querySelectorAll(".filters button").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".filters button").forEach(x=>x.classList.remove("active"));b.classList.add("active");filter=b.dataset.filter;render()}));search.addEventListener("input",render);